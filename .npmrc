# Supply-chain quarantine.
# `min-release-age` is respected by pnpm (7 day quarantine). npm ignores it and
# warns; the actual enforcement for `npm install` runs via `scripts/check-package-age.mjs`
# invoked from the `preinstall` script in package.json.
min-release-age=7
